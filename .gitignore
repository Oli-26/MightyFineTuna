# Data + generated state — never commit working files at repo root
/*.jsonl
/*.jsonl.bak
/reviewed.txt
/llama.log
/batch.log
*.log

# But DO commit shared/aggregated data dropped in results/
# (prefs.jsonl files from each rater — see README "Sharing data")
!results/
!results/*.jsonl

# Models
*.gguf

# Python / venvs
__pycache__/
.venv/
*.pyc

# Editor / OS junk
.vscode/
.idea/
.DS_Store

# Training outputs
/checkpoints/
